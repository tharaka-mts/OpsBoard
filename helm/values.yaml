backend:
  image:
    repository: <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/opsboard-backend
    tag: "prod-v1"
    pullPolicy: IfNotPresent
  replicaCount: 1
  containerPort: 8080
  servicePort: 8080

frontend:
  image:
    repository: <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/opsboard-frontend
    tag: "prod-v1"
    pullPolicy: IfNotPresent
  replicaCount: 1
  containerPort: 80
  servicePort: 80

postgres:
  image:
    repository: postgres
    tag: "16-alpine"
    pullPolicy: IfNotPresent
  port: 5432
  database: opsboard
  username: opsboard_user
  passwordPlaceholder: "replace-me"
  storageSize: 1Gi

ingress:
  enabled: true
  className: nginx
  host: opsboard.local
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # cert-manager.io/cluster-issuer: "letsencrypt-prod"

config:
  springProfilesActive: "prod"
