apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd
data:
  # This is a minimal placeholder ConfigMap for ArgoCD notifications.
  # Currently, no specific triggers are configured as per the initial requirements,
  # keeping this file ready for later extensions (e.g. Slack/Email webhooks).

  # Example outline for future use:
  # trigger.on-sync-failed: |
  #   - when: app.status.sync.status == 'OutOfSync'
  #     send: [slack]
  placeholder: "true"
